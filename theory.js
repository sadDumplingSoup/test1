(function () {
  "use strict";

  const lesson = (config) => config;
  const formula = (name, expression, explanation, symbols) => ({ name, expression, explanation, symbols });
  const step = (title, description, expression, note) => ({ title, description, expression, note });

  window.theoryLessons = {
    "梯度下降": lesson({
      readTime: 18,
      prerequisites: ["函数与导数", "偏导数", "损失函数", "向量基础"],
      intuition: "把损失函数想成一片山地，模型参数是你当前所在的位置，梯度指出当前位置上升最快的方向，因此沿着负梯度方向迈一小步，就有机会让损失下降。",
      objectives: ["理解梯度、学习率和损失曲面的关系", "能逐步写出一次参数更新", "能判断学习率过大、过小以及梯度接近零的现象"],
      steps: [
        step("定义模型与参数", "先明确模型包含哪些待学习参数。例如直线模型 ŷ = wx + b 中，w 和 b 就是需要寻找的参数。"),
        step("选择损失函数", "用一个标量衡量当前预测有多差。回归常用均方误差，分类常用交叉熵。损失越小通常表示预测越接近目标。", "J(w,b) = (1/n) Σ(ŷᵢ-yᵢ)²"),
        step("前向计算损失", "使用当前参数完成预测，并把所有样本的误差汇总成当前损失 J。"),
        step("计算参数梯度", "分别求损失对每个参数的偏导数。∂J/∂w 表示 w 轻微增加时，损失变化得多快。", "∇J = [∂J/∂w, ∂J/∂b]"),
        step("沿负梯度更新", "用学习率 η 控制步长。减号非常关键，因为梯度指向上升方向，而我们需要下降。", "θ ← θ - η∇J(θ)", "η 太大可能越过最低点甚至发散；太小则训练很慢。"),
        step("重复直到停止", "持续执行预测、计算损失、求梯度和更新参数，直到损失变化很小、达到最大轮数或验证集不再改善。")
      ],
      formulas: [
        formula("通用更新公式", "θₜ₊₁ = θₜ - η∇θJ(θₜ)", "在第 t 次迭代的参数基础上，减去学习率乘以当前损失梯度。", [["θ", "模型的全部参数"], ["η", "学习率，决定单步更新幅度"], ["J", "损失函数"], ["∇θJ", "J 对参数 θ 的梯度向量"]]),
        formula("线性回归中 w 的梯度", "∂J/∂w = (2/n) Σ(ŷᵢ-yᵢ)xᵢ", "误差会乘以输入 x：输入越大，同样的预测误差对斜率 w 的影响越大。", [["n", "样本数量"], ["ŷᵢ-yᵢ", "第 i 个样本的预测误差"], ["xᵢ", "第 i 个输入"]])
      ],
      example: { title: "只更新一次会发生什么？", description: "设一个样本 x=2、y=5，模型 ŷ=wx，当前 w=1，学习率 η=0.1。", calculations: ["当前预测：ŷ = 1×2 = 2", "平方误差：J = (2-5)² = 9", "梯度：∂J/∂w = 2(2-5)×2 = -12", "更新：w ← 1 - 0.1×(-12) = 2.2", "新预测：ŷ = 2.2×2 = 4.4，已经更接近 5"] },
      pseudocode: ["随机初始化参数 θ", "重复执行：", "  使用 θ 计算预测值", "  计算当前损失 J", "  计算梯度 gradients", "  θ = θ - learning_rate × gradients", "  检查停止条件"],
      strengths: ["概念通用，几乎所有神经网络都依靠它训练", "适合无法直接求解析解的高维问题", "可用小批量数据平衡速度与稳定性"],
      limitations: ["对学习率敏感", "非凸问题不保证找到全局最优", "可能遇到鞍点、梯度消失或梯度爆炸"],
      pitfalls: ["忘记特征缩放，导致不同参数更新速度差异巨大", "只看训练损失，不看验证集表现", "把一次波动误认为训练失败；小批量损失本来就会抖动"],
      checks: [["为什么更新公式中是减号？", "因为梯度指向函数增长最快的方向，负梯度才是局部下降最快的方向。"], ["学习率越大是否一定越快？", "不是。过大会反复越过最低点，甚至使损失持续增大。"]]
    }),

    "一元与多元线性回归": lesson({
      readTime: 20,
      prerequisites: ["向量与矩阵", "均值与方差", "梯度下降", "训练集与测试集"],
      intuition: "线性回归用一组特征的加权和预测连续数值。每个权重描述在其他条件不变时，对应特征增加一个单位会让预测改变多少。",
      objectives: ["理解系数、截距和残差", "掌握从数据到评价的完整训练步骤", "能解释 MSE、R² 和正则化的作用"],
      steps: [
        step("准备输入与目标", "把每个样本表示为特征向量 x，把房价、销量等连续目标表示为 y。类别特征需要编码，尺度差异很大时建议标准化。"),
        step("写出线性假设", "模型假设目标是特征的加权和再加一个截距。多元回归只是把一个 x 扩展为多个特征。", "ŷᵢ = w₁xᵢ₁ + ... + wₚxᵢₚ + b"),
        step("计算残差", "每个样本的残差 eᵢ = yᵢ-ŷᵢ。残差为正表示模型低估，为负表示模型高估。"),
        step("最小化平方误差", "训练时寻找让所有残差平方和最小的参数。平方会抵消正负并加重惩罚大误差。", "MSE = (1/n)Σ(yᵢ-ŷᵢ)²"),
        step("求解参数", "低维问题可用正规方程直接求解；大规模问题常用梯度下降。Scikit-learn 会选择稳定的数值求解方式。"),
        step("检查假设与泛化", "在测试集计算 MAE、RMSE、R²，并绘制残差图。若残差呈弯曲或漏斗形，线性或等方差假设可能不合适。"),
        step("必要时加入正则化", "Ridge 在损失中加入权重平方和以缩小系数；Lasso 加入绝对值和，可让部分系数变为零。")
      ],
      formulas: [
        formula("矩阵形式", "ŷ = Xw + b", "X 的每行是一个样本，每列是一个特征；一次矩阵乘法即可得到全部预测。", [["X", "n×p 的特征矩阵"], ["w", "p 维权重向量"], ["b", "截距"], ["ŷ", "n 个预测值"]]),
        formula("决定系数", "R² = 1 - Σ(yᵢ-ŷᵢ)² / Σ(yᵢ-ȳ)²", "比较模型误差与“永远预测均值”的基线误差。1 表示完美；0 表示与均值基线相当；负数表示更差。", [["ȳ", "目标值均值"], ["Σ(yᵢ-ŷᵢ)²", "模型残差平方和"], ["Σ(yᵢ-ȳ)²", "总平方和"]]),
        formula("Ridge 目标", "J = MSE + λΣwⱼ²", "λ 越大，越强烈地压缩权重，有助于降低方差但可能增加偏差。", [["λ", "正则化强度"], ["Σwⱼ²", "所有权重的平方和"]])
      ],
      example: { title: "解释房价模型", description: "模型为房价 = 0.8×面积 - 2×房龄 + 30（单位经过统一）。", calculations: ["面积系数 0.8：其他条件不变，面积每增加 1 单位，预测房价增加 0.8", "房龄系数 -2：其他条件不变，房龄每增加 1 单位，预测房价降低 2", "截距 30：全部特征为 0 时的数学基准，不一定有真实业务意义", "注意：相关性不等于因果性，系数不能自动证明面积导致价格变化"] },
      pseudocode: ["划分训练集和测试集", "仅用训练集拟合预处理器", "训练 LinearRegression 或 Ridge", "在测试集生成预测", "计算 MAE、RMSE、R²", "绘制残差并检查异常点", "解释系数及其限制"],
      strengths: ["快速、可解释，是优秀基线模型", "训练稳定，适合样本较少的数据", "可通过正则化缓解多重共线性"],
      limitations: ["只能直接表达线性关系", "对异常值较敏感", "特征高度相关时系数可能不稳定"],
      pitfalls: ["在全部数据上标准化后再划分，造成数据泄漏", "只报告 R² 而忽略误差实际单位", "把系数解释为因果效应"],
      checks: [["为什么 MSE 对异常值敏感？", "误差被平方，大误差会获得远高于小误差的权重。"], ["R²=0.8 是否意味着预测准确率 80%？", "不是。它表示相对均值基线解释了约 80% 的目标变异，不是分类准确率。"]]
    }),

    "逻辑回归": lesson({
      readTime: 18,
      prerequisites: ["线性回归", "概率", "对数", "交叉熵"],
      intuition: "先像线性回归一样计算加权和，再用 Sigmoid 把任意实数压缩为 0～1 的概率。它虽然叫“回归”，实际是经典分类算法。",
      objectives: ["理解概率、阈值和决策边界", "理解二元交叉熵为什么惩罚自信的错误", "能根据业务代价调整分类阈值"],
      steps: [
        step("编码类别与准备特征", "二分类常把正类记为 1、负类记为 0；数值特征通常标准化，类别特征进行编码。"),
        step("计算线性得分", "把各特征加权求和。z 还不是概率，可以取任意实数。", "z = wᵀx + b"),
        step("映射成概率", "使用 Sigmoid 将 z 转成正类概率。z=0 时概率为 0.5；z 越大，概率越接近 1。", "p(y=1|x) = σ(z) = 1/(1+e⁻ᶻ)"),
        step("计算交叉熵损失", "真实为 1 时希望 p 接近 1；真实为 0 时希望 p 接近 0。对数让自信但错误的预测受到很大惩罚。"),
        step("优化权重", "通过梯度下降或数值优化算法最小化全部样本的平均交叉熵，可加入 L1/L2 正则化。"),
        step("选择决策阈值", "默认 p≥0.5 判为正类，但疾病筛查常降低阈值提高召回率，昂贵人工复核则可能提高阈值。"),
        step("使用合适指标评价", "结合混淆矩阵、Precision、Recall、F1、ROC-AUC 或 PR-AUC，而非只看准确率。")
      ],
      formulas: [
        formula("Sigmoid", "σ(z) = 1 / (1 + e⁻ᶻ)", "把线性得分平滑地映射到概率区间，并保持单调性。", [["z", "线性得分 wᵀx+b"], ["e", "自然常数"]]),
        formula("二元交叉熵", "L = -[y log(p) + (1-y)log(1-p)]", "当 y=1 时只剩 -log(p)；当 y=0 时只剩 -log(1-p)。正确类别概率越低，损失越大。", [["y", "真实标签 0 或 1"], ["p", "预测为正类的概率"]]),
        formula("对数几率", "log(p/(1-p)) = wᵀx+b", "逻辑回归假设概率的对数几率与特征呈线性关系。", [["p/(1-p)", "odds，事件发生与不发生的概率比"]])
      ],
      example: { title: "为什么阈值会改变结果？", description: "某患者患病概率为 0.42。", calculations: ["阈值 0.5：0.42<0.5，预测为未患病", "阈值 0.3：0.42≥0.3，预测为患病并进入复查", "降低阈值通常减少漏诊、提高 Recall，但会增加误报、降低 Precision", "最佳阈值应由漏诊和误报的实际代价决定"] },
      pseudocode: ["标准化训练特征", "初始化 w 和 b", "循环：计算 z → Sigmoid 概率 → 交叉熵 → 梯度 → 更新参数", "在验证集上选择阈值", "在测试集报告概率指标和分类指标"],
      strengths: ["速度快、概率输出易解释", "配合正则化可处理高维稀疏特征", "适合作为分类基线"],
      limitations: ["决策边界本质上是线性的", "复杂非线性关系需要特征构造", "完全分离数据可能导致系数无限增大"],
      pitfalls: ["把 0.5 当成永远正确的阈值", "类别严重不平衡时只看 Accuracy", "没有标准化就直接比较系数大小"],
      checks: [["逻辑回归为何不用 MSE 作为首选损失？", "交叉熵与伯努利概率模型匹配，优化性质更好，并强烈惩罚自信的错误。"], ["Sigmoid 输出是否天然是校准良好的概率？", "不一定，仍需在验证数据上检查校准曲线。"]]
    }),

    "K 近邻 KNN": lesson({
      readTime: 14,
      prerequisites: ["欧氏距离", "特征缩放", "分类与回归"],
      intuition: "要判断一个新样本，就观察训练集中离它最近的 K 个邻居，让邻居投票或取平均。KNN 几乎不训练，而把主要计算推迟到预测阶段。",
      objectives: ["掌握 KNN 的完整预测过程", "理解 K 值和距离度量的影响", "理解为什么必须关注特征尺度"],
      steps: [
        step("保存训练样本", "KNN 不拟合显式参数，训练阶段主要是存储已标注数据。"),
        step("统一特征尺度", "距离直接受数值大小影响。年龄 0～100 与收入 0～100000 一起使用时，收入会主导距离，因此通常先标准化。"),
        step("计算新样本到所有训练样本的距离", "最常用欧氏距离，也可根据数据选择曼哈顿距离、余弦距离等。"),
        step("选出最近的 K 个邻居", "按距离排序并取前 K 个。K 小时边界灵活但易受噪声影响；K 大时更平滑但可能忽略局部结构。"),
        step("聚合邻居答案", "分类采用多数投票；回归采用目标均值。也可让距离更近的邻居拥有更大权重。"),
        step("交叉验证选择 K", "在验证折中比较多个 K，避免用测试集挑选 K。二分类常尝试奇数 K 以减少平票。")
      ],
      formulas: [
        formula("欧氏距离", "d(x,z) = √Σⱼ(xⱼ-zⱼ)²", "计算两个样本在特征空间中的直线距离。", [["j", "特征维度"], ["xⱼ,zⱼ", "两个样本在第 j 个特征上的值"]]),
        formula("距离加权投票", "weightᵢ = 1/(dᵢ+ε)", "距离越近权重越大；ε 防止距离为零时除零。", [["dᵢ", "第 i 个邻居的距离"], ["ε", "很小的稳定常数"]])
      ],
      example: { title: "K=3 的分类", description: "新样本最近的三个标签依次为 A、A、B。", calculations: ["不加权投票：A 得 2 票，B 得 1 票，因此预测 A", "若 B 距离极近，可使用距离加权使 B 的影响变大", "当 K 从 3 改为 9，结果可能因更远邻居加入而改变"] },
      pseudocode: ["对训练特征拟合标准化器", "转换训练样本与新样本", "计算新样本到每个训练样本的距离", "选取距离最小的 K 个", "分类投票或回归取均值", "返回预测"],
      strengths: ["直觉简单，无复杂训练过程", "可表达非线性决策边界", "自然支持多分类"],
      limitations: ["预测慢且需要存储全部训练数据", "高维空间中距离会失去区分力", "对尺度、噪声和无关特征敏感"],
      pitfalls: ["忘记标准化", "在测试集上选择 K", "数据量很大或维度很高时仍盲目使用"],
      checks: [["K 太小通常会怎样？", "模型方差较高，容易受单个噪声点影响而过拟合。"], ["为什么高维会伤害 KNN？", "高维中样本普遍变得稀疏，最近与最远距离的差异缩小。"]]
    }),

    "决策树": lesson({
      readTime: 20,
      prerequisites: ["分类与回归", "概率", "过拟合"],
      intuition: "决策树像一套按顺序执行的问答规则：年龄是否小于 30？收入是否高于某值？每次选择最能让子节点更纯的提问。",
      objectives: ["理解节点分裂与纯度", "能解释一条预测路径", "理解树深度、剪枝与过拟合"],
      steps: [
        step("从全部训练样本建立根节点", "初始节点包含所有样本，先计算其中类别混杂程度或回归目标方差。"),
        step("生成候选分裂", "对每个特征尝试若干阈值，例如年龄≤30 与年龄>30，得到左右两个子节点。"),
        step("计算每个分裂的收益", "分类比较分裂前后 Gini 或熵的下降；回归比较均方误差或方差的下降。"),
        step("选择收益最大的分裂", "采用贪心策略选当前最优的特征与阈值。注意局部最优不保证整棵树全局最优。"),
        step("递归创建子树", "在左右子节点重复候选分裂、计算收益和选择最优分裂。"),
        step("满足条件时停止", "达到最大深度、节点样本过少、纯度足够高或没有有效收益时，创建叶节点。"),
        step("叶节点给出预测", "分类输出多数类别或类别比例；回归通常输出节点内目标均值。"),
        step("剪枝与验证", "限制 max_depth、min_samples_leaf，或先长出大树再剪枝，以提高对新数据的泛化。")
      ],
      formulas: [
        formula("Gini 不纯度", "Gini = 1 - Σₖpₖ²", "随机抽取一个样本并按节点类别分布随机赋类时，分错的概率。节点只有一个类别时为 0。", [["pₖ", "节点中第 k 类样本比例"]]),
        formula("信息熵", "H = -Σₖpₖlog₂pₖ", "类别越混杂，熵越大；只有一个类别时熵为 0。", [["pₖ", "第 k 类的概率"]]),
        formula("分裂收益", "Gain = I(parent) - (nL/n)I(left) - (nR/n)I(right)", "用子节点样本数加权后的不纯度下降量。", [["I", "Gini、熵或回归误差"], ["nL,nR", "左右子节点样本数"]])
      ],
      example: { title: "一次 Gini 分裂", description: "父节点有 6 个正类、4 个负类。候选分裂后，左节点为 4 正 0 负，右节点为 2 正 4 负。", calculations: ["父节点 Gini = 1-(0.6²+0.4²)=0.48", "左节点 Gini = 0", "右节点 Gini = 1-[(2/6)²+(4/6)²]≈0.444", "加权子节点不纯度 = 4/10×0 + 6/10×0.444≈0.267", "收益 = 0.48-0.267=0.213，因此这是一个有效分裂"] },
      pseudocode: ["function grow(node):", "  若满足停止条件：返回叶节点", "  枚举所有特征与候选阈值", "  计算每个候选的加权不纯度", "  选择不纯度下降最大的候选", "  grow(左子节点)", "  grow(右子节点)"],
      strengths: ["规则直观，可处理非线性和特征交互", "无需标准化", "同时支持数值与类别任务"],
      limitations: ["单棵树方差高，数据轻微变化可能改变结构", "贪心建树不保证全局最优", "轴对齐分裂有时效率较低"],
      pitfalls: ["不限制深度直到叶节点纯净", "用训练准确率判断树好坏", "把 feature_importances_ 当成因果重要性"],
      checks: [["纯叶节点是否意味着模型很好？", "不一定，可能只是记住训练数据，测试集上反而表现差。"], ["树模型为何通常不要求标准化？", "分裂只依赖特征值的排序与阈值，线性缩放不改变排序。"]]
    }),

    "随机森林": lesson({
      readTime: 17,
      prerequisites: ["决策树", "过拟合", "Bagging", "偏差与方差"],
      intuition: "一棵深树容易受训练数据偶然性影响；随机森林让许多略有不同的树独立判断，再通过集体投票减少单棵树的波动。",
      objectives: ["理解 Bootstrap、随机特征与聚合", "解释随机森林为何降低方差", "掌握 OOB 评估和重要性限制"],
      steps: [
        step("Bootstrap 抽样", "对每棵树从 n 个训练样本中有放回抽取 n 次，因此一些样本会重复，另一些样本不会进入该树。"),
        step("生长一棵较深的树", "使用该 Bootstrap 样本训练决策树，通常不进行强剪枝。"),
        step("每个节点随机选择候选特征", "分裂时只考察全部特征的随机子集，使不同树不总被同一个强特征主导，从而降低树之间的相关性。"),
        step("重复训练多棵树", "每棵树拥有不同样本和特征随机性，形成多样化模型集合。"),
        step("聚合预测", "分类采用多数投票或平均类别概率；回归对所有树预测取平均。"),
        step("用 OOB 样本评价", "没有进入某棵树的样本称为袋外样本，可让这些树共同预测该样本，得到近似验证分数。")
      ],
      formulas: [
        formula("回归聚合", "ŷ = (1/B)ΣᵦTᵦ(x)", "对 B 棵树的预测取平均，独立误差会相互抵消。", [["B", "树的数量"], ["Tᵦ(x)", "第 β 棵树的预测"]]),
        formula("分类聚合", "ŷ = mode{T₁(x),...,Tᴮ(x)}", "选择所有树投票最多的类别。", [["mode", "众数，即出现次数最多的值"]])
      ],
      example: { title: "为什么投票更稳定？", description: "5 棵树对同一患者预测为：阳性、阳性、阴性、阳性、阴性。", calculations: ["阳性 3 票，阴性 2 票，最终预测阳性", "若单棵树因少量噪声判断错误，只要多数树保持正确，整体结果不变", "但如果所有树高度相似，它们会一起犯错，所以需要随机特征降低相关性"] },
      pseudocode: ["for b in 1...B:", "  从训练集 Bootstrap 抽样", "  训练决策树；每个节点只看随机特征子集", "对新样本收集全部树预测", "分类投票 / 回归平均"],
      strengths: ["强大稳健，默认参数通常已有不错表现", "能处理非线性、交互和混合尺度", "对异常值和噪声相对稳健"],
      limitations: ["模型大、推理比单树慢", "不如单棵树容易解释", "外推连续数值能力弱"],
      pitfalls: ["树数量太少导致结果不稳定", "盲信基于不纯度的重要性，它偏爱高基数特征", "时间序列数据错误地随机抽样和划分"],
      checks: [["随机森林主要降低偏差还是方差？", "主要通过平均多棵去相关的树来降低方差。"], ["树越多会导致过拟合吗？", "通常增加树数会让结果趋于稳定而非明显过拟合，但会增加计算与内存。"]]
    }),

    "支持向量机 SVM": lesson({
      readTime: 21,
      prerequisites: ["向量点积", "线性分类", "距离", "正则化"],
      intuition: "SVM 不只找一条能分开类别的线，还希望这条线与两侧最近样本之间的间隔尽可能宽。离边界最近、真正决定边界的样本叫支持向量。",
      objectives: ["理解最大间隔与支持向量", "解释 C 和核函数", "区分硬间隔、软间隔和核 SVM"],
      steps: [
        step("定义分隔超平面", "线性边界由 wᵀx+b=0 给出，两侧符号决定类别。"),
        step("计算函数间隔", "对标签 y∈{-1,+1}，希望 y(wᵀx+b) 为正且足够大。"),
        step("最大化几何间隔", "固定函数间隔尺度后，最小化 ||w||² 等价于让两侧边界之间的几何距离最大。"),
        step("允许少量违反", "真实数据通常不能完美线性分开，引入松弛变量 ξ 允许样本进入间隔甚至被错分。"),
        step("用 C 平衡间隔与错误", "C 大时重罚训练错误，边界更贴合数据；C 小时更重视宽间隔和正则化。"),
        step("需要时使用核技巧", "核函数直接计算样本在高维映射后的内积，无需显式构造高维特征，从而得到非线性边界。"),
        step("缩放与调参", "SVM 强烈依赖距离和点积，因此要标准化，并用交叉验证选择 C、γ 和核。")
      ],
      formulas: [
        formula("软间隔目标", "min ½||w||² + CΣᵢξᵢ", "第一项追求宽间隔，第二项惩罚违反间隔的样本。", [["w", "超平面法向量"], ["C", "违反间隔的惩罚强度"], ["ξᵢ", "第 i 个样本的松弛量"]]),
        formula("约束", "yᵢ(wᵀxᵢ+b) ≥ 1-ξᵢ", "正确且位于间隔外时 ξ=0；进入间隔或错分时 ξ>0。", [["yᵢ", "-1 或 +1 标签"]]),
        formula("RBF 核", "K(x,z)=exp(-γ||x-z||²)", "距离近的样本相似度接近 1，距离远则接近 0。γ 越大，单个样本影响范围越小，边界越曲折。", [["γ", "RBF 核的影响范围参数"]])
      ],
      example: { title: "C 与 γ 如何影响边界", description: "在二维双月数据上使用 RBF SVM。", calculations: ["C 很大：模型尽量不犯训练错误，可能形成复杂边界", "C 较小：允许一些错误以换取更平滑边界", "γ 很大：每个样本只影响很小邻域，容易过拟合", "γ 很小：影响范围过宽，边界可能过于简单"] },
      pseudocode: ["标准化训练特征", "选择线性核或 RBF 核", "交叉验证搜索 C 与 γ", "求解最大间隔优化问题", "保留支持向量与对应系数", "使用支持向量计算新样本决策得分"],
      strengths: ["中小型高维数据上表现强", "只由支持向量决定边界", "核方法能表达复杂非线性"],
      limitations: ["大样本训练成本高", "参数和核选择较敏感", "原始输出不是概率"],
      pitfalls: ["未标准化特征", "把 C 误解为正则化强度；实际上 C 越大正则约束相对越弱", "高维大数据仍盲目使用 RBF 核"],
      checks: [["哪些样本决定最终边界？", "主要是位于间隔附近或违反间隔的支持向量。"], ["γ 增大通常让边界怎样变化？", "影响范围缩小，边界更局部、更复杂，过拟合风险上升。"]]
    }),

    "K-Means 聚类": lesson({
      readTime: 18,
      prerequisites: ["向量距离", "均值", "特征标准化", "无监督学习"],
      intuition: "先放置 K 个中心，每个样本归到最近中心，再把中心移动到所属样本的平均位置；反复执行直到分组不再明显改变。",
      objectives: ["完整描述分配与更新循环", "理解目标函数和初始化", "知道何时 K-Means 不适用"],
      steps: [
        step("选择 K 并标准化特征", "K 是预先指定的群组数量。距离会受尺度影响，因此通常先标准化。"),
        step("初始化 K 个中心", "随机初始化可能得到差的局部最优，实践中常用 K-Means++ 让初始中心彼此分散。"),
        step("分配步骤", "计算每个样本到全部中心的距离，把样本分给最近中心。"),
        step("更新步骤", "对每个簇内样本取均值，用该均值替换旧中心。均值正是平方距离和最小的位置。"),
        step("检查收敛", "如果中心移动小于阈值、标签不再变化或达到最大迭代次数，就停止；否则回到分配步骤。"),
        step("多次初始化", "用不同初始中心重复训练，保留簇内平方和最小的结果，以降低局部最优影响。"),
        step("评价与解释", "结合肘部法、轮廓系数、稳定性和业务含义选择 K。没有真实标签时，单个指标不是绝对答案。")
      ],
      formulas: [
        formula("聚类目标", "J = ΣₖΣxᵢ∈Cₖ ||xᵢ-μₖ||²", "最小化每个样本到所属簇中心的平方距离总和，也叫 inertia。", [["Cₖ", "第 k 个簇的样本集合"], ["μₖ", "第 k 个簇中心"]]),
        formula("中心更新", "μₖ = (1/|Cₖ|)Σxᵢ∈Cₖ xᵢ", "新中心是该簇全部样本逐维取平均。", [["|Cₖ|", "第 k 个簇的样本数"]]),
        formula("轮廓系数", "s=(b-a)/max(a,b)", "a 是样本与同簇样本的平均距离，b 是与最近其他簇的平均距离；越接近 1 越好。", [["a", "簇内紧密度"], ["b", "与最近其他簇的分离度"]])
      ],
      example: { title: "一次分配与更新", description: "一维样本为 1、2、8、9，K=2，初始中心为 2 和 8。", calculations: ["分配：1、2 更接近中心 2；8、9 更接近中心 8", "更新：第一簇中心=(1+2)/2=1.5", "第二簇中心=(8+9)/2=8.5", "再次分配不变，算法收敛"] },
      pseudocode: ["选择 K，初始化 centers", "repeat:", "  labels = 每个样本最近的 center", "  new_centers = 每个簇的样本均值", "  shift = centers 与 new_centers 的移动量", "  centers = new_centers", "until shift 很小"],
      strengths: ["简单快速，易扩展到大数据", "结果中心便于形成群组画像", "球状、大小相近的簇效果好"],
      limitations: ["必须预先指定 K", "只适合近似球状、方差相近的簇", "对异常值和初始化敏感"],
      pitfalls: ["未标准化直接聚类", "只凭肘部图机械选择 K", "把簇编号 0、1、2 当成有大小顺序"],
      checks: [["为什么更新中心要取均值？", "均值是使簇内平方距离总和最小的点。"], ["月牙形簇适合 K-Means 吗？", "通常不适合，因为最近中心划分倾向形成凸的、近似球状区域。"]]
    }),

    "PCA 主成分分析": lesson({
      readTime: 20,
      prerequisites: ["矩阵", "方差与协方差", "特征值与特征向量", "标准化"],
      intuition: "如果二维数据大致沿一条斜线分布，可以旋转坐标轴，让第一根新轴沿数据延展最长的方向，再丢掉变化很小的第二根轴。",
      objectives: ["理解主成分、解释方差和投影", "掌握 PCA 的标准步骤", "避免数据泄漏并正确解释成分"],
      steps: [
        step("标准化或中心化", "至少减去各特征均值。特征单位不同时通常还要除以标准差，否则数值尺度大的特征会主导方差。"),
        step("计算协方差矩阵", "协方差描述不同特征是否共同变化，矩阵对角线是各特征方差。"),
        step("求特征值与特征向量", "协方差矩阵的特征向量给出新坐标轴方向，特征值表示沿该方向的数据方差。实践中常用更稳定的 SVD。"),
        step("按特征值排序", "特征值越大，该方向保留的数据变化越多，依次称为第一、第二主成分。"),
        step("选择保留维数", "根据累计解释方差，例如保留能解释 95% 方差的最少主成分。"),
        step("把数据投影到新轴", "用中心化后的 X 乘以选中的特征向量矩阵，得到低维表示 Z。"),
        step("只在训练集拟合", "均值、标准差和主成分方向都只能从训练集学习，再用于验证集和测试集。")
      ],
      formulas: [
        formula("协方差矩阵", "C = (1/(n-1))XᵀX", "这里 X 已按列中心化，C 的形状为 p×p。", [["n", "样本数量"], ["p", "特征数量"]]),
        formula("特征分解", "Cv = λv", "方向 v 经过协方差矩阵变换后方向不变，缩放量 λ 就是该方向的方差。", [["v", "主成分方向"], ["λ", "对应解释方差"]]),
        formula("低维投影", "Z = XWₖ", "Wₖ 的列是前 k 个主成分，把 p 维 X 投影成 k 维 Z。", [["Wₖ", "p×k 的主成分矩阵"]])
      ],
      example: { title: "如何选择主成分数", description: "前四个成分解释方差比例为 60%、25%、10%、5%。", calculations: ["保留 1 个成分：累计 60%", "保留 2 个成分：累计 85%", "保留 3 个成分：累计 95%", "若目标是保留至少 90% 方差，应选择 3 个成分"] },
      pseudocode: ["在训练集拟合 StandardScaler", "转换训练集", "计算 SVD 或协方差特征分解", "按解释方差排序", "选择前 k 个方向", "投影训练、验证、测试数据"],
      strengths: ["压缩维度、去除线性相关", "可降低噪声和训练成本", "便于二维、三维可视化"],
      limitations: ["主成分是原特征混合，可解释性下降", "只捕捉线性结构", "高方差不一定代表对预测最有用"],
      pitfalls: ["在全部数据上拟合 PCA", "未缩放量纲不同的特征", "把 PCA 图上的分离直接当成类别因果证据"],
      checks: [["PCA 是有监督算法吗？", "不是。它不使用标签，只寻找输入数据方差最大的方向。"], ["第一主成分对应原始某一个特征吗？", "通常不是，它一般是多个原始特征的线性组合。"]]
    }),

    "反向传播": lesson({
      readTime: 22,
      prerequisites: ["导数与偏导数", "链式法则", "矩阵乘法", "前向传播", "损失函数"],
      intuition: "前向传播从输入计算到损失；反向传播则从损失开始追问：每个中间量改变一点，会让最终损失改变多少？链式法则把这种影响逐层传回。",
      objectives: ["理解计算图和局部梯度", "能手算一个两步复合函数的梯度", "理解梯度累计和自动微分"],
      steps: [
        step("构建前向计算图", "把线性变换、激活函数和损失拆成一系列简单运算，并保存前向计算需要的中间值。"),
        step("从损失开始", "损失对自身的导数为 1，这是反向传播的起点。", "∂L/∂L = 1"),
        step("计算当前节点的局部导数", "每个运算只需知道自己的输出对输入如何变化，例如 z=wx+b，则 ∂z/∂w=x。"),
        step("使用链式法则向前一层传递", "上游梯度乘以本节点的局部导数，得到损失对更早变量的梯度。", "∂L/∂w = (∂L/∂z)(∂z/∂w)"),
        step("分支处累计梯度", "如果一个变量影响多条后续路径，损失对它的总梯度是各条路径梯度之和。"),
        step("得到所有参数梯度", "按计算图逆序处理，直到权重与偏置都有对应梯度。"),
        step("优化器更新参数", "反向传播只负责求梯度；SGD、Adam 等优化器负责用梯度更新参数。"),
        step("清空梯度进入下一批次", "PyTorch 默认累积梯度，因此每批训练通常需要 optimizer.zero_grad()。")
      ],
      formulas: [
        formula("链式法则", "dy/dx = (dy/du)(du/dx)", "复合函数 y=f(u)、u=g(x) 中，x 对 y 的影响等于两段局部影响相乘。", [["u", "中间变量"]]),
        formula("线性层反传", "∂L/∂W = (∂L/∂z)xᵀ", "上游梯度与输入的外积给出权重矩阵梯度。", [["z", "Wx+b 的输出"], ["∂L/∂z", "从后层传来的梯度"]]),
        formula("ReLU 导数", "d ReLU(z)/dz = 1(z>0), 否则 0", "正区间梯度通过，负区间梯度被截断。", [["1(·)", "条件成立为 1，否则为 0"]])
      ],
      example: { title: "手算一条计算链", description: "令 x=2、w=3，z=wx=6，y=z²=36。求 dy/dw。", calculations: ["局部导数 dy/dz = 2z = 12", "局部导数 dz/dw = x = 2", "链式法则 dy/dw = 12×2 = 24", "直接展开 y=(wx)²，dy/dw=2wx²=2×3×4=24，结果一致"] },
      pseudocode: ["prediction = model(x)  # 前向", "loss = criterion(prediction, y)", "optimizer.zero_grad()", "loss.backward()       # 反向传播", "optimizer.step()      # 更新参数"],
      strengths: ["以与前向传播同阶的成本计算大量参数梯度", "适用于任意可微计算图", "自动微分框架可准确实现复杂模型"],
      limitations: ["深层网络可能梯度消失或爆炸", "不可微操作需要替代或特殊处理", "保留中间值会占用显存"],
      pitfalls: ["忘记清空累积梯度", "在推理阶段仍保留梯度图浪费内存", "误以为 backward() 会自动更新参数"],
      checks: [["反向传播与梯度下降有什么区别？", "反向传播计算梯度；梯度下降使用这些梯度更新参数。"], ["一个变量分叉到两条路径时如何处理？", "将两条路径传回的梯度相加。"]]
    }),

    "卷积核与特征图": lesson({
      readTime: 20,
      prerequisites: ["矩阵", "图像通道", "全连接层", "反向传播"],
      intuition: "一个小卷积核像可学习的滤镜，在图像各局部位置重复扫描；浅层可能识别边缘，深层再把边缘组合成纹理、部件和物体。",
      objectives: ["能手算二维卷积", "理解通道、步长、填充和特征图", "理解局部连接与参数共享"],
      steps: [
        step("确定输入与卷积核形状", "输入常表示为 C_in×H×W；每个输出通道拥有 C_in 个 k×k 核。"),
        step("把核放在一个局部窗口", "核覆盖输入的一小块区域，对应元素相乘后求和，再加偏置，得到输出的一个像素。"),
        step("按步长滑动", "步长 S 决定每次移动几个像素。S 越大，输出空间尺寸越小。"),
        step("处理边界", "填充 P 在图像外围补零。3×3 核、步长 1、填充 1 通常可保持高宽不变。"),
        step("生成一个输出通道", "同一组卷积核参数在所有位置共享，因此同一种模式无论出现在图像哪里都能被检测。"),
        step("使用多组核", "每组核学习一种不同模式，产生多张特征图，组成 C_out×H_out×W_out 输出。"),
        step("激活与堆叠", "卷积后通常接 ReLU、归一化或池化；多层堆叠扩大感受野并形成层次特征。")
      ],
      formulas: [
        formula("二维卷积位置输出", "Yₒ,ᵢ,ⱼ = bₒ + ΣcΣuΣv Wₒ,c,u,v Xc,i+u,j+v", "对所有输入通道和核窗口元素做乘加，得到输出通道 o 在位置 (i,j) 的值。", [["W", "可学习卷积核"], ["bₒ", "输出通道偏置"], ["c", "输入通道索引"]]),
        formula("输出尺寸", "Hout = ⌊(H+2P-K)/S⌋+1", "由输入高 H、填充 P、核大小 K 和步长 S 决定。宽度同理。", [["⌊·⌋", "向下取整"]]),
        formula("参数数量", "Params = Cout(CinK²+1)", "与图像高宽无关，这是参数共享带来的优势；+1 表示每个输出通道的偏置。", [["Cin,Cout", "输入与输出通道数"]])
      ],
      example: { title: "3×3 输入与 2×2 卷积核", description: "输入 [[1,2,0],[0,1,3],[2,1,0]]，核 [[1,0],[0,-1]]，步长 1、无填充。", calculations: ["左上窗口 [[1,2],[0,1]]：1×1+2×0+0×0+1×(-1)=0", "右上窗口 [[2,0],[1,3]]：2×1+0+0-3=-1", "输出尺寸：(3-2)/1+1=2，因此得到 2×2 特征图", "同一个核会继续用于下方两个窗口，这就是参数共享"] },
      pseudocode: ["for each output_channel:", "  for each output position (i,j):", "    value = bias", "    for each input_channel:", "      value += sum(window * kernel)", "    output[channel,i,j] = value"],
      strengths: ["保留图像空间结构", "局部连接和参数共享大幅减少参数", "对平移具有一定等变性"],
      limitations: ["标准卷积对旋转、尺度变化不天然不变", "深层模型计算量仍可能很大", "局部感受野捕获全局关系需要堆叠多层"],
      pitfalls: ["混淆输入通道与输出通道", "输出尺寸计算漏掉填充或向下取整", "把卷积核理解为人工固定滤镜；CNN 中它通常由训练学习"],
      checks: [["为什么参数量不随输入图像高宽增加？", "同一卷积核在所有空间位置共享参数。"], ["3×3 核如何保持尺寸？", "步长 1 时在四周填充 1 个像素。"]]
    }),

    "LSTM": lesson({
      readTime: 22,
      prerequisites: ["RNN", "Sigmoid 与 Tanh", "梯度消失", "序列数据"],
      intuition: "LSTM 在普通 RNN 隐藏状态之外增加一条细胞状态通道，并用三个门控制遗忘、写入和输出，让重要信息更容易跨越较长时间。",
      objectives: ["理解遗忘门、输入门和输出门", "逐步描述单个时间步计算", "区分细胞状态 cₜ 与隐藏状态 hₜ"],
      steps: [
        step("拼接当前输入与旧隐藏状态", "把 xₜ 与 hₜ₋₁ 合并，作为三个门和候选记忆的共同输入。"),
        step("遗忘门决定保留多少旧记忆", "Sigmoid 输出 0～1；接近 0 表示遗忘，接近 1 表示保留。", "fₜ = σ(Wf[hₜ₋₁,xₜ]+bf)"),
        step("输入门决定写入多少", "输入门 iₜ 控制候选内容 gₜ 中哪些部分写入细胞状态。", "iₜ=σ(Wi[hₜ₋₁,xₜ]+bi)"),
        step("生成候选记忆", "Tanh 产生 -1～1 的候选信息，包含当前输入与历史上下文提取的新内容。", "gₜ=tanh(Wg[hₜ₋₁,xₜ]+bg)"),
        step("更新细胞状态", "一部分旧记忆被保留，一部分新记忆被加入。逐元素加法为梯度提供更直接的传播路径。", "cₜ=fₜ⊙cₜ₋₁+iₜ⊙gₜ"),
        step("输出门选择对外暴露的信息", "输出门决定当前细胞记忆的哪些部分形成隐藏状态。", "oₜ=σ(Wo[hₜ₋₁,xₜ]+bo)"),
        step("得到当前隐藏状态", "先把细胞状态压缩到 -1～1，再乘输出门。hₜ用于当前预测并传给下一时间步。", "hₜ=oₜ⊙tanh(cₜ)")
      ],
      formulas: [
        formula("细胞状态更新", "cₜ=fₜ⊙cₜ₋₁+iₜ⊙gₜ", "第一项保留历史，第二项写入新内容；⊙ 表示逐元素乘法。", [["cₜ", "长期记忆状态"], ["fₜ", "遗忘门"], ["iₜ", "输入门"], ["gₜ", "候选记忆"]]),
        formula("隐藏状态", "hₜ=oₜ⊙tanh(cₜ)", "隐藏状态是经过输出门筛选的当前可见信息。", [["oₜ", "输出门"], ["hₜ", "短期输出/隐藏状态"]])
      ],
      example: { title: "情感分析中的记忆", description: "句子：“这部电影虽然很长，但一点也不无聊”。", calculations: ["读到“很长”时模型可能保存轻微负面信号", "读到“但”时输入门写入转折提示，遗忘门可能降低前面负面信息", "读到“不无聊”时模型结合否定关系写入正面候选记忆", "最终隐藏状态更倾向正面情感"] },
      pseudocode: ["h, c = 初始零向量", "for token in sequence:", "  f = sigmoid(...)", "  i = sigmoid(...); g = tanh(...)", "  c = f*c + i*g", "  o = sigmoid(...)", "  h = o*tanh(c)"],
      strengths: ["比普通 RNN 更能建模长期依赖", "门控机制可学习保留与遗忘", "适合中等长度序列和时间序列"],
      limitations: ["时间步必须顺序计算，难以完全并行", "超长序列仍可能困难", "参数和计算量高于普通 RNN"],
      pitfalls: ["把 cₜ 与 hₜ 当成同一个量", "忘记处理变长序列的 padding/mask", "文本任务中忽略词表外和序列截断影响"],
      checks: [["哪一个状态更像长期记忆通道？", "细胞状态 cₜ。"], ["三个门为何使用 Sigmoid？", "因为需要生成 0～1 的软控制比例。"]]
    }),

    "Query、Key 与 Value": lesson({
      readTime: 18,
      prerequisites: ["向量点积", "Softmax", "词向量", "矩阵乘法"],
      intuition: "Query 表示当前位置想找什么，Key 表示每个位置能用什么特征被匹配，Value 表示匹配后实际取回的信息。它类似用搜索词与索引匹配，再读取对应内容。",
      objectives: ["解释 Q、K、V 的角色", "能手算缩放点积注意力", "理解为什么同一输入要经过三个不同线性变换"],
      steps: [
        step("把输入映射为 Q、K、V", "同一组 token 表示 X 分别乘三个可学习矩阵，让模型学会用于查询、匹配和传递的不同表示。", "Q=XWQ, K=XWK, V=XWV"),
        step("计算 Query 与所有 Key 的相似度", "使用点积 QKᵀ。点积越大，表示当前 Query 与对应 Key 越匹配。"),
        step("进行缩放", "除以 √dₖ，防止维度大时点积绝对值变大，使 Softmax 过度饱和、梯度过小。"),
        step("应用 Mask", "需要时给不允许关注的位置加负无穷，例如 Decoder 中阻止看到未来 token。"),
        step("Softmax 得到权重", "每一行权重均为非负且和为 1，表示一个 Query 对所有位置分配多少注意力。"),
        step("加权汇总 Value", "权重矩阵乘 V，得到每个位置融合上下文后的新表示。"),
        step("端到端学习投影矩阵", "WQ、WK、WV 通过最终任务损失反向传播自动学习，无需人工规定语义。")
      ],
      formulas: [
        formula("缩放点积注意力", "Attention(Q,K,V)=softmax(QKᵀ/√dₖ)V", "先根据 Q 与 K 得到关系权重，再用权重对 V 求加权和。", [["dₖ", "Key 向量维度"], ["QKᵀ", "所有查询与键的两两相似度矩阵"]]),
        formula("线性投影", "Q=XWQ, K=XWK, V=XWV", "三个矩阵独立学习，使匹配依据与被提取内容可以不同。", [["X", "输入序列表示"], ["WQ,WK,WV", "可学习参数矩阵"]])
      ],
      example: { title: "两词注意力的简化计算", description: "一个 Query 对两个 Key 的缩放分数为 [2,1]，对应 Value 为 [10,0] 与 [0,6]。", calculations: ["Softmax([2,1])≈[0.731,0.269]", "第一个位置得到约 73.1% 注意力，第二个得到约 26.9%", "输出 = 0.731×[10,0] + 0.269×[0,6]", "输出 ≈ [7.31,1.61]，同时融合两个 Value，但偏重第一个"] },
      pseudocode: ["Q = X @ Wq; K = X @ Wk; V = X @ Wv", "scores = Q @ K.transpose(-2,-1)", "scores = scores / sqrt(d_k)", "scores += mask", "weights = softmax(scores)", "output = weights @ V"],
      strengths: ["任意两个位置可直接交互", "计算可并行", "权重可用于分析模型关注关系"],
      limitations: ["标准自注意力的内存和计算随序列长度平方增长", "注意力权重不等同于因果解释", "缺少位置编码时不理解顺序"],
      pitfalls: ["混淆 Key 和 Value 的作用", "忘记除以 √dₖ", "Mask 方向或广播维度写错"],
      checks: [["为什么不能只用一个 V 做相似度？", "匹配依据和要提取的内容可能不同，分离 K 与 V 提供了更灵活的表示。"], ["Softmax 在哪个维度做？", "通常对每个 Query 对应的全部 Key 分数做，使其权重和为 1。"]]
    }),

    "Transformer Encoder": lesson({
      readTime: 24,
      prerequisites: ["Q、K、V", "多头注意力", "位置编码", "残差连接", "Layer Normalization"],
      intuition: "Encoder 层先让每个 token 从全句收集相关信息，再让每个位置通过同一个小型前馈网络独立加工；残差和归一化保证深层堆叠仍可训练。",
      objectives: ["按顺序讲清 Encoder 层", "理解多头注意力、残差和 FFN 的分工", "能追踪张量形状"],
      steps: [
        step("词向量加入位置信息", "token embedding 表达内容，位置编码表达顺序；二者相加后形状仍为 batch×length×d_model。"),
        step("生成多个头的 Q、K、V", "每个头用独立投影在较低维子空间中学习不同关系，例如指代、局部搭配或长距离依赖。"),
        step("每个头执行自注意力", "序列自身同时提供 Q、K、V，每个位置聚合所有允许关注位置的信息。"),
        step("拼接并投影多头输出", "把各头结果在特征维拼接，再乘输出矩阵，回到 d_model 维。"),
        step("第一次残差与归一化", "将注意力输出与该子层输入相加，再做 LayerNorm；残差为信息和梯度提供捷径。"),
        step("逐位置前馈网络", "对每个 token 独立应用同一两层 MLP，通常先升维、激活，再降回 d_model。"),
        step("第二次残差与归一化", "把 FFN 输出与其输入相加并归一化，得到一层 Encoder 输出。"),
        step("堆叠多层", "后一层继续基于前一层的上下文表示建模，逐步形成高级语义。")
      ],
      formulas: [
        formula("多头注意力", "MultiHead(Q,K,V)=Concat(head₁,...,headₕ)Wᴼ", "不同头并行学习不同关系，最后拼接并混合。", [["h", "注意力头数量"], ["Wᴼ", "输出投影矩阵"]]),
        formula("前馈网络", "FFN(x)=W₂·GELU(W₁x+b₁)+b₂", "对每个位置独立应用，提供非线性特征变换；参数在位置之间共享。", [["W₁", "通常把维度扩大"], ["W₂", "投影回 d_model"]]),
        formula("残差子层", "y=LayerNorm(x+Sublayer(x))", "保留原输入并叠加子层学习的修正。不同实现也可能先归一化再进入子层。", [["Sublayer", "注意力或 FFN"]])
      ],
      example: { title: "形状如何变化", description: "batch=8，序列长=128，d_model=512，8 个头。", calculations: ["输入 X：[8,128,512]", "每头 Q/K/V：[8,8,128,64]，因为 512/8=64", "注意力分数：[8,8,128,128]", "各头输出拼接后：[8,128,512]", "FFN 可先变为 [8,128,2048]，再回到 [8,128,512]", "残差相加要求输入输出最后一维相同"] },
      pseudocode: ["x = token_embedding + position_embedding", "a = multi_head_self_attention(x)", "x = layer_norm(x + dropout(a))", "f = feed_forward(x)", "x = layer_norm(x + dropout(f))", "return x"],
      strengths: ["并行处理序列", "直接建模长距离关系", "容易扩展模型与数据规模"],
      limitations: ["注意力复杂度为 O(n²)", "训练需要大量数据与计算", "结构本身不含顺序，必须注入位置信息"],
      pitfalls: ["多头拼接维度计算错误", "忘记 padding mask", "混淆 Post-LN 与 Pre-LN 实现顺序"],
      checks: [["注意力后为什么还能做 FFN？", "注意力负责跨位置混合信息，FFN 负责每个位置内部的非线性特征变换。"], ["为什么残差两侧形状必须相同？", "残差需要逐元素相加。"]]
    }),

    "贝尔曼最优方程": lesson({
      readTime: 21,
      prerequisites: ["MDP", "回报与折扣因子", "状态价值", "动作价值", "期望"],
      intuition: "一个状态的最优价值，可以拆成“现在采取某动作得到的奖励”与“到达下一状态后仍能获得的最优未来价值”；选择其中最好的动作即可。",
      objectives: ["理解一步展开和递归结构", "区分 V* 与 Q* 方程", "能在小型 MDP 中执行一次价值备份"],
      steps: [
        step("确定当前状态 s", "假设智能体位于 s，需要比较所有可选动作。"),
        step("枚举一个动作 a", "动作可能随机转移到不同下一状态 s′，并产生奖励 r。"),
        step("计算一步奖励", "先考虑执行 a 后的即时收益，它由奖励函数给出。"),
        step("加入折扣后的未来价值", "下一状态仍会按最优方式行动，因此加入 γV*(s′)。γ 控制未来奖励的重要程度。"),
        step("对环境随机性求期望", "如果动作结果不确定，要用转移概率对所有 s′、r 加权平均。"),
        step("在全部动作中取最大", "最优价值定义为当前可选动作里期望回报最高的那个。"),
        step("反复进行价值备份", "价值迭代从任意初值开始对所有状态重复更新，直到数值收敛。"),
        step("提取最优策略", "收敛后，在每个状态选择使贝尔曼表达式最大的动作。")
      ],
      formulas: [
        formula("最优状态价值", "V*(s)=maxₐ Σs′,r p(s′,r|s,a)[r+γV*(s′)]", "先对一个动作的随机结果求期望，再选择期望回报最大的动作。", [["p(s′,r|s,a)", "转移到 s′ 并获得 r 的概率"], ["γ", "折扣因子"]]),
        formula("最优动作价值", "Q*(s,a)=Σs′,r p(s′,r|s,a)[r+γ maxₐ′Q*(s′,a′)]", "已固定当前动作 a，因此最大化发生在下一状态的动作选择上。", [["a′", "下一状态可选动作"]]),
        formula("贪心策略", "π*(s)=argmaxₐ Q*(s,a)", "选择 Q* 最大的动作就得到一个最优策略。", [["argmax", "返回使表达式最大的位置/动作"]])
      ],
      example: { title: "一次价值备份", description: "状态 s 有左、右两个确定动作。左：奖励 1，下一状态价值 4；右：奖励 0，下一状态价值 7；γ=0.9。", calculations: ["左动作价值 = 1 + 0.9×4 = 4.6", "右动作价值 = 0 + 0.9×7 = 6.3", "V*(s)=max(4.6,6.3)=6.3", "当前最优动作是向右，尽管即时奖励更低，因为长期价值更高"] },
      pseudocode: ["初始化 V(s)=0", "repeat:", "  for each state s:", "    对每个动作计算期望 [r + γV(s')]", "    V_new(s) = 最大动作值", "until max|V_new-V| < tolerance", "策略取每个状态的最大动作"],
      strengths: ["把长期决策分解为可重复的一步备份", "是动态规划、Q-Learning 等算法的理论核心", "有限折扣 MDP 下具有良好收敛性质"],
      limitations: ["精确动态规划需要已知转移概率", "大状态空间无法逐状态保存与更新", "最大化可能带来价值高估"],
      pitfalls: ["忘记对随机转移求期望", "把即时奖励最大动作当成长期最优", "混淆 V* 中当前 max 与 Q* 中下一步 max"],
      checks: [["γ=0 时智能体关心什么？", "只关心当前即时奖励。"], ["贝尔曼方程为什么是递归的？", "当前价值的定义包含下一状态的价值，而后者又包含更后续状态的价值。"]]
    }),

    "SARSA": lesson({
      readTime: 17,
      prerequisites: ["Q 函数", "TD Error", "ε-Greedy", "On-policy 与 Off-policy"],
      intuition: "SARSA 按照智能体实际执行的探索策略学习：当前状态 S 采取 A，得到 R 到达 S′，再按当前策略选出实际下一动作 A′，用这五元组更新。",
      objectives: ["理解 SARSA 五元组", "掌握 On-policy 更新", "能解释它与 Q-Learning 的行为差异"],
      steps: [
        step("初始化 Q 表", "为每个状态-动作对设置初值，常从 0 开始。"),
        step("在状态 S 按 ε-Greedy 选择 A", "大部分时间利用当前最大 Q 动作，小概率随机探索。"),
        step("执行 A", "环境返回奖励 R、下一状态 S′ 以及是否终止。"),
        step("在 S′ 仍按 ε-Greedy 选择 A′", "A′ 可能是探索动作，这是 On-policy 的关键。"),
        step("构造 TD 目标", "使用实际选出的 A′ 的价值，而不是所有动作中的最大值。", "target=R+γQ(S′,A′)"),
        step("计算 TD Error 并更新", "目标与当前估计之差决定更新方向。", "Q(S,A)←Q(S,A)+α[target-Q(S,A)]"),
        step("状态动作一起前移", "令 S←S′、A←A′，继续交互，直到回合结束。")
      ],
      formulas: [
        formula("SARSA 更新", "Q(S,A)←Q(S,A)+α[R+γQ(S′,A′)-Q(S,A)]", "括号内为 TD Error；更新目标包含当前行为策略真正选择的 A′。", [["α", "学习率"], ["γ", "折扣因子"], ["A′", "按当前策略选出的下一动作"]])
      ],
      example: { title: "悬崖边的安全路线", description: "ε-Greedy 在悬崖边可能随机走入悬崖。", calculations: ["SARSA 的目标包含实际探索动作造成的风险", "靠近悬崖的状态价值会因偶发跌落而降低", "因此它常学到离悬崖更远、回报略低但更安全的路线", "这体现它学习的是当前含探索行为策略的价值"] },
      pseudocode: ["初始化 Q", "for each episode:", "  S = reset(); A = epsilon_greedy(Q[S])", "  repeat:", "    R,S' = env.step(A)", "    A' = epsilon_greedy(Q[S'])", "    Q[S,A] += α(R+γQ[S',A']-Q[S,A])", "    S,A = S',A'"],
      strengths: ["更新简单，无需环境模型", "学习时会考虑探索策略带来的风险", "表格任务中稳定直观"],
      limitations: ["大状态空间需要函数近似", "每次更新依赖下一实际动作", "探索率不衰减时学到的是持续探索策略"],
      pitfalls: ["更新前重新随机选择多次 A′", "终止状态仍加入下一状态价值", "误以为 On-policy 表示不能使用旧经验"],
      checks: [["SARSA 名字来自哪五个量？", "State、Action、Reward、next State、next Action。"], ["为何它被称为 On-policy？", "行为和学习目标都使用同一个当前策略。"]]
    }),

    "Q-Learning": lesson({
      readTime: 20,
      prerequisites: ["MDP", "动作价值 Q", "TD Learning", "ε-Greedy", "贝尔曼最优方程"],
      intuition: "智能体可以用 ε-Greedy 到处探索，但更新时假设下一步会选择价值最大的动作，因此它一边用探索策略收集经验，一边学习最终贪心策略。",
      objectives: ["逐步实现 Q-Learning", "解释 TD 目标和 Off-policy", "理解 α、γ、ε 各自影响"],
      steps: [
        step("初始化 Q 表", "Q(s,a) 存储在状态 s 执行动作 a 后的长期回报估计。未访问状态可从 0 开始。"),
        step("观察当前状态 S", "从环境 reset 得到起始状态，或从上一步得到新状态。"),
        step("用 ε-Greedy 选择行为动作 A", "概率 1-ε 选当前最大 Q 动作，概率 ε 随机探索。此动作负责收集数据。"),
        step("与环境交互", "执行 A，得到即时奖励 R、下一状态 S′ 和终止标志。"),
        step("计算最优 TD 目标", "不管行为策略下一步会做什么，目标都使用 S′ 中最大的 Q 值。", "target=R+γ maxₐ′Q(S′,a′)"),
        step("计算 TD Error", "目标减当前估计，正值表示之前低估，负值表示高估。", "δ=target-Q(S,A)"),
        step("更新一个 Q 单元格", "沿 TD Error 方向移动 α 比例。", "Q(S,A)←Q(S,A)+αδ"),
        step("进入下一状态并重复", "到达终止状态时，下一状态价值按 0 处理；跨回合逐渐降低 ε，常实现从探索到利用。")
      ],
      formulas: [
        formula("Q-Learning 更新", "Q(s,a)←Q(s,a)+α[r+γ maxₐ′Q(s′,a′)-Q(s,a)]", "用一步真实奖励与下一状态的最优估计修正当前动作价值。", [["α", "学习率，控制相信新经验的程度"], ["γ", "折扣因子"], ["ε", "仅用于行为选择，不直接出现在更新式中"]]),
        formula("ε-Greedy", "A = random，概率 ε；argmaxₐQ(s,a)，概率 1-ε", "用随机探索避免永远困在早期看似最好的动作。", [["ε", "探索概率"]])
      ],
      example: { title: "手算一次更新", description: "当前 Q(s,a)=2，奖励 r=1，下一状态最大 Q=4，α=0.5，γ=0.9。", calculations: ["TD 目标 = 1 + 0.9×4 = 4.6", "TD Error = 4.6-2 = 2.6", "更新量 = 0.5×2.6 = 1.3", "新 Q(s,a)=2+1.3=3.3", "估计向新目标 4.6 移动了一半，而不是直接替换"] },
      pseudocode: ["初始化 Q[state, action]", "for each episode:", "  state = env.reset()", "  while not done:", "    action = epsilon_greedy(Q[state])", "    next_state,reward,done = env.step(action)", "    target = reward if done else reward+γ*max(Q[next_state])", "    Q[state,action] += α*(target-Q[state,action])", "    state = next_state"],
      strengths: ["无需环境转移模型", "实现简单，理论清晰", "可直接学习最优贪心策略"],
      limitations: ["Q 表无法处理巨大或连续状态", "样本效率可能低", "max 操作可能造成价值高估"],
      pitfalls: ["终止状态仍加 γmaxQ", "ε 一开始就太小导致探索不足", "把 α 与 ε 混淆：一个控制学习，一个控制探索"],
      checks: [["Q-Learning 为何是 Off-policy？", "行为可以是 ε-Greedy，但更新目标使用贪心的 max 动作，两者不是同一策略。"], ["α=1 表示什么？", "完全用当前一次 TD 目标替换旧估计，忽略过去经验。"]]
    }),

    "Deep Q-Network": lesson({
      readTime: 24,
      prerequisites: ["Q-Learning", "神经网络", "反向传播", "经验回放", "目标网络"],
      intuition: "当状态太多无法做 Q 表时，用神经网络输入状态，一次输出所有离散动作的 Q 值。经验回放和目标网络用于处理神经网络训练中的相关性与目标漂移。",
      objectives: ["掌握 DQN 完整训练循环", "理解回放缓冲区和目标网络", "能写出 DQN 损失"],
      steps: [
        step("建立在线 Q 网络", "输入状态 s，输出每个离散动作的 Q(s,a)。只取实际执行动作对应的输出参与当前样本损失。"),
        step("用 ε-Greedy 选择动作", "以 ε 随机探索，否则选择在线网络输出最大的动作。"),
        step("存储转移", "把 (s,a,r,s′,done) 放入固定容量的 Replay Buffer，旧数据超容量后被覆盖。"),
        step("随机采样小批量", "从缓冲区均匀随机抽取转移，打破连续时间步的强相关，并让同一经验被多次复用。"),
        step("计算当前 Q", "在线网络计算 Q_online(s)，再按动作 a gather 出 Q_online(s,a)。"),
        step("用目标网络构造 TD 目标", "目标网络参数暂时固定，计算 r+γmax Q_target(s′,a′)；终止样本只用 r。"),
        step("最小化 TD 损失", "使用 MSE 或更稳健的 Huber Loss，让当前 Q 逼近 TD 目标。目标值计算时不传播梯度。"),
        step("周期同步目标网络", "每隔 C 步把在线网络参数复制给目标网络，或用软更新缓慢跟随。"),
        step("逐步衰减 ε 并评估", "训练时保留探索；评估时通常 ε=0，运行多个回合统计平均奖励。")
      ],
      formulas: [
        formula("DQN 目标", "y=r+γ(1-done)maxₐ′Q(s′,a′;θ⁻)", "终止时 (1-done)=0；θ⁻ 是目标网络参数。", [["θ⁻", "目标网络参数"], ["done", "终止标志 0/1"]]),
        formula("TD 损失", "L(θ)=E[(y-Q(s,a;θ))²]", "只更新在线网络 θ，使其预测靠近相对稳定的 y。", [["θ", "在线网络参数"]]),
        formula("软更新", "θ⁻←τθ+(1-τ)θ⁻", "每步让目标网络向在线网络移动很小比例 τ。", [["τ", "通常很小，如 0.005"]])
      ],
      example: { title: "一个批次中的单条样本", description: "奖励 1，未终止，γ=0.99，目标网络下一状态最大 Q=5，在线网络当前 Q(s,a)=3。", calculations: ["目标 y = 1+0.99×5=5.95", "TD Error = 5.95-3=2.95", "平方损失 = 2.95²≈8.70", "反向传播会提高在线网络对当前 (s,a) 的估计", "目标网络在本次梯度更新中保持不变"] },
      pseudocode: ["初始化 online_net 与 target_net；复制参数", "for each environment step:", "  ε-greedy 选择 action", "  执行并存入 replay_buffer", "  batch = replay_buffer.sample()", "  q = online(batch.states).gather(actions)", "  with no_grad: target = r + γ*(1-done)*target_net(next).max()", "  loss = huber(q,target); backward; optimizer.step()", "  定期同步 target_net"],
      strengths: ["处理高维状态", "经验回放提高数据利用率", "开启了深度强化学习的经典路线"],
      limitations: ["基础 DQN 只适合离散动作", "训练对超参数和奖励尺度敏感", "价值估计可能不稳定或过高"],
      pitfalls: ["目标值没有 detach/no_grad", "训练刚开始缓冲区样本太少", "在线网络与目标网络每步完全同步，失去稳定作用", "把 next_state 的最大动作索引用于当前 state gather"],
      checks: [["为何不直接用连续最新经验训练？", "相邻经验高度相关，会破坏小批量梯度下降近似独立同分布的条件。"], ["目标网络为何能稳定训练？", "它让监督目标在若干更新内相对固定，减少追逐快速移动目标。"]]
    }),

    "REINFORCE": lesson({
      readTime: 19,
      prerequisites: ["参数化策略", "概率分布", "回报", "策略梯度"],
      intuition: "如果一次轨迹最终回报高，就提高轨迹中已采取动作的概率；回报低则降低这些动作概率。它直接优化策略，不需要 Q 表。",
      objectives: ["理解采样轨迹与蒙特卡洛回报", "掌握 log-probability 目标", "理解高方差和 baseline"],
      steps: [
        step("建立策略网络", "输入状态 s，输出离散动作概率或连续动作分布参数。"),
        step("按策略分布采样动作", "不是总选最大概率动作，而是从 πθ(a|s) 采样，以保持探索并得到正确梯度估计。"),
        step("收集完整回合", "记录每一步的状态、动作对数概率和奖励，直到终止。"),
        step("从后向前计算回报", "每个时间步 Gₜ 是从该步开始的折扣奖励和。"),
        step("构造策略损失", "回报高的动作应提高概率。优化器做最小化，因此使用负号：-Gₜ log πθ(aₜ|sₜ)。"),
        step("对整条轨迹求和并反传", "一次更新使用蒙特卡洛完整回报，没有自举偏差，但方差较高。"),
        step("加入 baseline 降低方差", "用 Gₜ-b(sₜ) 替代 Gₜ；只要 baseline 不依赖当前动作，梯度期望保持不变。"),
        step("标准化回报并重复采样", "实践中常对一批回报标准化，改善数值尺度，但不改变算法核心。")
      ],
      formulas: [
        formula("折扣回报", "Gₜ=Σₖ₌₀ᵀ⁻ᵗ γᵏrₜ₊ₖ", "从时间 t 开始累计未来奖励，越远奖励折扣越多。", [["γ", "折扣因子"]]),
        formula("REINFORCE 梯度", "∇θJ≈Σₜ Gₜ∇θlogπθ(aₜ|sₜ)", "用采样回报作为动作好坏的权重。", [["πθ", "由 θ 参数化的策略"]]),
        formula("最小化形式的损失", "L=-Σₜ(Gₜ-bₜ)logπθ(aₜ|sₜ)", "负号把最大化期望回报转成优化器可最小化的损失。", [["bₜ", "baseline"]])
      ],
      example: { title: "回报如何改变动作概率", description: "在某状态选择“向右”的概率为 0.4。", calculations: ["若该轨迹回报很高，G>0，梯度更新会提高 log π(向右|s)，从而提高其概率", "若回报为负，会降低该动作概率", "但一条高回报轨迹不证明每一步都好，这是 REINFORCE 方差高的原因之一"] },
      pseudocode: ["for each episode:", "  trajectory = []", "  while not done:", "    dist = policy(state)", "    action = dist.sample()", "    保存 log_prob 与 reward", "  反向计算每步 return", "  loss = -sum(return * log_prob)", "  backward and update"],
      strengths: ["直接优化随机策略", "可用于连续或离散动作", "概念和实现相对简洁"],
      limitations: ["必须等待完整回合", "蒙特卡洛估计方差高", "样本利用率低"],
      pitfalls: ["用 argmax 代替采样导致策略梯度失效", "漏掉损失负号", "把环境奖励张量保留在错误设备或图中", "回报跨回合串联计算"],
      checks: [["为什么使用 log π 而不是直接 π？", "对数技巧把轨迹概率的乘积梯度转成各时间步梯度之和，并得到稳定形式。"], ["baseline 会改变最优策略吗？", "合适的、与当前动作无关的 baseline 不改变梯度期望，只降低方差。"]]
    }),

    "Actor-Critic": lesson({
      readTime: 22,
      prerequisites: ["REINFORCE", "价值函数", "TD Error", "优势函数", "神经网络"],
      intuition: "Actor 负责提出动作，Critic 像教练一样估计当前局面有多好；实际结果比 Critic 预期好，就鼓励 Actor 的动作，反之则抑制。",
      objectives: ["区分 Actor 与 Critic 的输入输出", "理解 TD Error 作为优势估计", "掌握两个损失的更新逻辑"],
      steps: [
        step("Actor 输出策略", "给定状态 s，Actor 输出 πθ(a|s)，并按该分布采样动作。"),
        step("环境产生转移", "执行动作得到奖励 r、下一状态 s′ 和终止标志。"),
        step("Critic 估计状态价值", "Critic Vφ(s) 预测从当前状态继续执行策略的期望回报。"),
        step("构造一步 TD 目标", "未终止时目标为 r+γVφ(s′)，终止时只有 r。"),
        step("计算 TD Error/优势", "δ=r+γV(s′)-V(s)。δ>0 表示结果比预期好。"),
        step("更新 Critic", "最小化 δ²，让价值预测更接近 TD 目标；目标分支通常停止梯度。"),
        step("更新 Actor", "使用 -δ logπθ(a|s)；δ 为正则提高动作概率，为负则降低。"),
        step("加入熵奖励", "可在 Actor 损失中鼓励较高策略熵，避免过早变得完全确定而停止探索。")
      ],
      formulas: [
        formula("TD Error", "δₜ=rₜ+γVφ(sₜ₊₁)-Vφ(sₜ)", "作为当前动作优势的低方差近似。", [["φ", "Critic 参数"]]),
        formula("Actor 损失", "Lactor=-logπθ(aₜ|sₜ)·stopgrad(δₜ)", "δ 只作为权重，不应通过 Actor 损失反向修改 Critic。", [["stopgrad", "停止梯度"]]),
        formula("Critic 损失", "Lcritic=(r+γVφ(s′)-Vφ(s))²", "训练价值函数逼近一步自举目标。", [["γVφ(s′)", "对未来回报的估计"]])
      ],
      example: { title: "Critic 如何指导 Actor", description: "Critic 预测当前价值 3，执行动作后奖励 1，下一状态价值 4，γ=0.9。", calculations: ["TD 目标=1+0.9×4=4.6", "δ=4.6-3=1.6，为正", "Critic 会把 V(s) 向 4.6 提高", "Actor 会提高刚才动作在 s 中的概率，因为结果好于预期"] },
      pseudocode: ["action, log_prob = actor.sample(state)", "next_state,reward,done = env.step(action)", "value = critic(state)", "with no_grad: target = reward+γ*(1-done)*critic(next_state)", "advantage = target-value", "critic_loss = advantage²", "actor_loss = -log_prob*advantage.detach()", "分别反向并更新"],
      strengths: ["比纯蒙特卡洛策略梯度方差低", "可在线、逐步更新", "是 A2C、PPO、SAC 等算法基础"],
      limitations: ["Critic 不准确会给 Actor 错误方向", "自举引入偏差", "双网络训练可能不稳定"],
      pitfalls: ["Actor 损失中的 advantage 未 detach", "终止状态仍使用 V(s′)", "Actor 和 Critic 学习率设置完全不顾尺度", "误把 Q、V、Advantage 混为一谈"],
      checks: [["δ>0 对 Actor 意味着什么？", "当前动作结果好于 Critic 预期，应提高该动作概率。"], ["为什么 Critic 能降低方差？", "它提供状态相关基线，使 Actor 关注相对预期的好坏，而非波动很大的绝对回报。"]]
    }),

    "PPO": lesson({
      readTime: 25,
      prerequisites: ["Actor-Critic", "优势函数", "重要性采样", "策略梯度", "GAE"],
      intuition: "策略更新太猛可能让新策略突然失去旧策略已经学会的行为。PPO 比较新旧策略对同一动作的概率，并把变化比例限制在一个安全区间。",
      objectives: ["理解采样、优势估计和多轮更新", "解释概率比与裁剪目标", "掌握 PPO 的 Actor、Critic 和熵损失"],
      steps: [
        step("用旧策略采集一批轨迹", "运行当前策略 π_old，保存状态、动作、奖励、终止、旧动作对数概率和价值估计。PPO 属于 On-policy 方法。"),
        step("计算回报与优势", "常用 GAE 把多步 TD Error 加权汇总，在偏差与方差之间折中。"),
        step("标准化优势", "对批次优势减均值除标准差，使优化尺度更稳定。"),
        step("计算新旧策略概率比", "在同一批旧数据上，用当前新参数重新计算动作概率。", "rₜ(θ)=πθ(aₜ|sₜ)/πold(aₜ|sₜ)"),
        step("构造未裁剪目标", "rₜAₜ 表示新策略相对旧策略改变动作概率后带来的代理收益。"),
        step("裁剪过大的策略变化", "把概率比限制在 [1-ε,1+ε]，取未裁剪与裁剪目标的较小值，阻止从过大更新中继续获益。"),
        step("同时训练 Critic", "让价值网络拟合计算出的回报目标，提供之后更好的优势估计。"),
        step("加入熵奖励并组合损失", "熵鼓励探索；总损失通常由策略损失、价值损失和熵项加权组成。"),
        step("小批量重复多个 Epoch", "同一批 On-policy 数据可复用若干轮，但过多会让新策略离采样策略太远，失去可靠性。"),
        step("丢弃旧批次并重新采样", "更新结束后用新策略采集下一批数据，重复流程。")
      ],
      formulas: [
        formula("概率比", "rₜ(θ)=exp(logπθ(aₜ|sₜ)-logπold(aₜ|sₜ))", "用对数概率相减再指数化，数值上比直接相除更稳定。", [["rₜ", "新旧策略对已执行动作的相对概率"]]),
        formula("裁剪代理目标", "Lclip=E[min(rₜAₜ, clip(rₜ,1-ε,1+ε)Aₜ)]", "根据优势正负自动限制提高或降低动作概率的幅度。", [["Aₜ", "优势估计"], ["ε", "裁剪范围，常见如 0.1～0.2"]]),
        formula("GAE", "Aₜ=Σₗ₌₀∞(γλ)ˡδₜ₊ₗ", "累积多个未来 TD Error；λ 越大更接近蒙特卡洛、方差更高，越小更依赖 Critic。", [["δ", "一步 TD Error"], ["λ", "GAE 衰减参数"]])
      ],
      example: { title: "裁剪如何生效", description: "某动作优势 A=2，旧概率 0.4，新概率 0.6，ε=0.2。", calculations: ["概率比 r=0.6/0.4=1.5", "允许上限 1+ε=1.2", "未裁剪目标=1.5×2=3.0", "裁剪目标=1.2×2=2.4", "取较小值 2.4，继续增大动作概率不会获得额外目标收益"] },
      pseudocode: ["用 policy_old 收集 rollout", "计算 returns 与 GAE advantages", "for epoch in update_epochs:", "  for minibatch:", "    ratio = exp(new_logprob-old_logprob)", "    policy_loss = -mean(min(ratio*A, clip(ratio)*A))", "    value_loss = mse(V, returns)", "    loss = policy_loss + c1*value_loss - c2*entropy", "    backward; clip_grad; step", "同步 old policy / 重新采样"],
      strengths: ["实现相对简单且训练稳定", "离散、连续动作都适用", "同一批数据可做有限次数复用"],
      limitations: ["仍是 On-policy，样本效率低于 SAC 等 Off-policy 算法", "对奖励尺度、优势计算和批次设置敏感", "裁剪只是启发式约束，不保证每次都改善"],
      pitfalls: ["old_logprob 在更新中被重新计算", "优势未标准化导致训练尺度不稳", "终止与时间截断没有区分", "对同一 rollout 更新过多 Epoch", "损失符号或熵项符号写反"],
      checks: [["A>0 时 PPO 想怎样改变动作概率？", "提高该动作概率，但概率比超过上限后收益会被裁剪。"], ["PPO 为何仍叫 On-policy？", "数据来自更新前的近期策略，更新若干轮后就丢弃并重新采样。"]]
    }),

    "SAC": lesson({
      readTime: 26,
      prerequisites: ["Actor-Critic", "连续动作", "Off-policy", "策略熵", "双 Q 网络"],
      intuition: "SAC 不只追求高回报，还奖励策略保持适当随机性。这样智能体会持续探索多个可行行为，并利用经验回放反复学习，特别适合连续控制。",
      objectives: ["理解最大熵目标", "掌握 Actor、双 Critic、目标网络与温度参数", "解释重参数化技巧"],
      steps: [
        step("建立随机 Actor", "Actor 输入状态，输出高斯分布的均值和标准差，从中采样连续动作，并经 tanh 限制到动作范围。"),
        step("建立两个 Q Critic", "Q₁、Q₂ 分别估计 (s,a) 的软价值，取较小值抑制过估计。"),
        step("建立目标 Critic", "复制两个 Q 网络作为缓慢更新的目标网络，用于构造稳定 TD 目标。"),
        step("与环境交互并存入回放", "SAC 是 Off-policy，可将转移存入 Replay Buffer 并多次复用。"),
        step("采样下一动作", "由当前 Actor 在 s′ 采样 a′，同时得到 logπ(a′|s′)，表示动作的不确定性。"),
        step("构造软 Q 目标", "未来价值不仅包含 Q，还减去 αlogπ。低概率动作 logπ 更负，因此 -αlogπ 提供熵奖励。"),
        step("更新两个 Critic", "分别最小化 Qᵢ(s,a) 与软 TD 目标之间的平方误差。"),
        step("更新 Actor", "采样当前动作，使 αlogπ(a|s)-min(Q₁,Q₂) 最小，即同时追求高 Q 与高熵。"),
        step("自动调整温度 α", "让策略熵接近目标熵；α 大更重视探索，α 小更重视回报。"),
        step("软更新目标网络", "用很小 τ 让目标 Critic 缓慢跟随在线 Critic。")
      ],
      formulas: [
        formula("最大熵目标", "J(π)=E[Σₜ γᵗ(rₜ+αH(π(·|sₜ)))]", "在环境奖励之外加入策略熵，鼓励多样化行为。", [["H", "策略熵"], ["α", "回报与熵的权衡温度"]]),
        formula("软 Q 目标", "y=r+γ(1-done)[min(Q₁⁻,Q₂⁻)(s′,a′)-αlogπ(a′|s′)]", "双目标 Q 取最小值降低高估，熵项鼓励随机策略。", [["Q⁻", "目标 Critic"]]),
        formula("Actor 损失", "Lπ=E[αlogπ(a|s)-min(Q₁,Q₂)(s,a)]", "最小化它等价于提高 Q，同时避免策略过早变得确定。", [["a", "通过重参数化采样的动作"]]),
        formula("重参数化", "a=tanh(μθ(s)+σθ(s)⊙ε), ε~N(0,I)", "把随机性移到独立噪声 ε，使动作仍可对 Actor 参数求梯度。", [["μ,σ", "Actor 输出的分布参数"]])
      ],
      example: { title: "温度 α 的作用", description: "两个动作的 Q 值接近，但一个动作概率已经很高。", calculations: ["α 较小：Actor 主要选择 Q 略高的动作，策略很快集中", "α 较大：熵奖励更重要，Actor 会继续尝试两种动作", "自动温度调节会根据当前熵与目标熵的差异改变 α", "探索不是额外 ε-Greedy，而是随机策略本身的一部分"] },
      pseudocode: ["初始化 actor, q1, q2, target_q1, target_q2, replay", "循环与环境交互并存储转移", "batch = replay.sample()", "with no_grad: next_a,next_logp = actor.sample(next_s)", "target = r+γ*(1-done)*(min(target_q)-α*next_logp)", "更新 q1,q2 拟合 target", "a,logp = actor.sample(s)", "更新 actor: mean(α*logp-min(q1,q2))", "更新 α 与软更新目标网络"],
      strengths: ["Off-policy，样本利用率高", "连续动作任务表现稳定强大", "最大熵机制提供良好探索"],
      limitations: ["实现比 PPO 复杂", "多个网络带来较高计算和内存成本", "奖励尺度和目标熵仍影响表现"],
      pitfalls: ["tanh 后忘记修正 log probability", "Actor 更新时没有冻结/正确处理 Critic 梯度", "目标 Q 没有停止梯度", "误用 ε-Greedy 给连续动作额外加随机动作"],
      checks: [["为什么使用两个 Q 网络？", "取较小估计可缓解价值过估计。"], ["SAC 的探索从哪里来？", "来自最大熵目标下的随机 Actor，而非必需的 ε-Greedy。"]]
    })
  };
})();
